import 'package:flutter/material.dart';

class AppColors {
  // primary and secondary palette
  static const mainBlue = Color(0xff5068b0);
  static const scaffoldBg = Color.fromARGB(255, 252, 252, 252);
}
